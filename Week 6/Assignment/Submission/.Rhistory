?rnorm
rnorm(100)
1:50
y <- matrix(1:100, 10, 10)
View(y)
y
x <- matrix(101:200, 10, 10)
x*y
x
y
x*y
x <- matrix(1:100, 10, 10)
y <- matrix(1:100, 10, 10)
x
y
x*y
x%*%y
knitr::opts_chunk$set(echo = TRUE)
ggplot(wings.df, aes(x = Length, sample = wings.df)) + stat_qq() + stat_qq_line()
ggplot(wings.df, aes(x = Length)) + stat_qq() + stat_qq_line()
knitr::opts_chunk$set(echo = TRUE)
wing.length <- scan("https://seattlecentral.edu/qelp/sets/057/s057.txt")
wings.df <- data.frame(Length = wing.length)
library(ggplot2)
mean(wings.df$Length)
# Plug-in
sqrt(mean(wings.df$Length^2) - mean(wings.df$Length)^2)
# Sample SD
sd(wings.df$Length)
summary(wings.df)
ggplot(wings.df, aes(x = Length)) + stat_ecdf() + ylab("Empirical CDF")
ggplot(wings.df, aes(x = Length)) + geom_histogram(breaks = seq(35.5, 55.5, 1))
ggplot(wings.df, aes(x = Length)) + geom_density() + ylab("Estimated PDF")
ggplot(wings.df, aes(x = Length)) + stat_qq() + stat_qq_line()
ggplot(wings.df, aes(x = Length)) + stat_qq() + stat_qq_line()
ggplot(wings.df, aes(sample = Length)) + stat_qq() + stat_qq_line()
pnorm(65.5, mean = 63.8, sd = 2.9)
pnorm(65.5, mean = 63.8, sd = 2.9) * 100
source("~/University/S520 Intro to Stats/Week 6/Assignment/Submission/Stats_PS5_Q5.R")
source("~/University/S520 Intro to Stats/Week 6/Assignment/Stats_PS5_Q5.R")
dataset <- read.csv("IUSalaries2023.csv")
dataset <- read.csv("IUSalaries2023.csv")
salaries <- dataset[,11]
salaries_sorted <- sort(salaries)
# Finding the position of Dr. Luen's salary in the dataset
rank <- which(salaries_sorted == 81902)
percentile_rank = (rank/3034) * 100
print(percentile_rank)
# Therefore the percentile rank of Dr. Luen's salary is 38%
dataset <- read.csv("IUSalaries2023.csv")
dataset <- read.csv("IUSalaries2023.csv")
salaries <- dataset[,11]
salaries_sorted <- sort(salaries)
# Finding the position of Dr. Luen's salary in the dataset
rank <- which(salaries_sorted == 81902)
percentile_rank = (rank/3034) * 100
# Reading the dataset
dataset <- read.csv("IUSalaries2023.csv")
# Assigning the salary column alone to a new variable
salaries <- dataset[,11]
setwd("~/University/S520 Intro to Stats/Week 6/Assignment/Submission")
# Reading the dataset
dataset <- read.csv("IUSalaries2023.csv")
# Assigning the salary column alone to a new variable
salaries <- dataset[,11]
# Sorting the dataset so I can find the correct position of Dr. Luen's salary relative to the whole dataset
salaries_sorted <- sort(salaries)
# Size of vector
print(size(vector))
# Size of vector
print(dim(vector))
# Size of vector
dim(vector)
# Size of vector
dim(salaries_sorted)
# Size of vector
print(dim(salaries_sorted))
# Size of vector
size <- dim(salaries_sorted)
size
salaries_sorted
cat(salaries_sorted)
cat(size)
# Size of vector
size <- length(salaries_sorted)
cat(size)
# Percentile Rank
percentile_rank = (rank/size) * 100
# Size of vector
size <- length(salaries_sorted)
# Percentile Rank
percentile_rank = (rank/size) * 100
# Percentile Rank
percentile_rank = (rank/cat(size)) * 100
# Finding the position of Dr. Luen's salary in the dataset
rank <- which(salaries_sorted == 81902)
# Size of vector
size <- length(salaries_sorted)
# Percentile Rank
# Finding the position of Dr. Luen's salary in the dataset
rank <- which(salaries_sorted == 81902)
# Size of vector
size <- length(salaries_sorted)
# Percentile Rank
percentile_rank = (rank/3034) * 100
# Therefore, the percentile rank of Dr. Luen's salary is 38%
print(percentile_rank)
cat("The size of the vector is = ", size, "\n")
# Finding the position of Dr. Luen's salary in the dataset
rank <- which(salaries_sorted == 81902)
# Size of vector
size <- length(salaries_sorted)
cat("The size of the vector is = ", size, "\n")
# Percentile Rank
percentile_rank = (rank/3034) * 100
# Therefore, the percentile rank of Dr. Luen's salary is 38%
print(percentile_rank)
# Finding the position of Dr. Luen's salary in the dataset
rank <- which(salaries_sorted == 81902)
# Size of vector
size <- length(salaries_sorted)
cat("The size of the vector is = ", size, "\n")
# Percentile Rank
percentile_rank = (rank/3034) * 100
# Therefore, the percentile rank of Dr. Luen's salary is 38%
print(percentile_rank)
